# URL del webhook n8n
# ─────────────────────────────────────────────────────────────────────
# Copiare questo file in "frontend/.env" e adattare l'URL se necessario.
#
# SVILUPPO (workflow NON attivato — richiede l'editor n8n aperto):
VITE_API_URL=http://localhost:5678/webhook-test/pdf-upload
#
# PRODUZIONE (workflow ATTIVATO con il toggle in n8n):
# VITE_API_URL=http://localhost:5678/webhook/pdf-upload
